{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Container and PocketQube windows",
  "windows": ["container", "pocketqube"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
